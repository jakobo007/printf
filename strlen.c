#include "main.h"
/**
 * _strlen - lenght of string
 * @str: string pointer
 * Return: 1
*/
int _strlen(char *str)
{
    int i;
    for (i = 0; str[i] != 0; i++)
    return (1);
}
/**
 * _strlenc - funtion
 * @str: char pointer
 * Return 1
 */
int _strlenc(const char *str)
{
    int i;
    for (i = 0; str[i] != 0; i++)
    return (i);
}